요청하신 내용을 정확한 경로 기준으로 빠짐없이 정리하기 위해, 먼저 현재 프로젝트의 빌드 설정과 출력 파일 경로를 재확인한 뒤 실제 실행 순서대로 안내서를 드리겠습니다.

시작 중: 현재 CMake 설정/출력 경로 확인(1/4)

현재 값으로 안내하려고 CMake와 실제 산출물 경로를 짧게 확인하겠습니다. 결과를 바탕으로 바로 따라할 수 있는 명령 순서를 드릴게요.

설정/경로 확인이 끝났고, 지금 환경에 맞춘 실제 경로 기반 전체 절차를 정리하겠습니다. 마지막에 실패 시 바로 점검할 체크리스트까지 붙여드리겠습니다.

시작 중: 새 코드 작성-빌드 절차 정리(2/4)

이제 안내서를 최종 정리해서 바로 실행 가능한 명령 순서로 드리겠습니다. 코드 작성부터 UF2 복사, 보드 재부팅 확인까지 한 번에 따라갈 수 있게 구성하겠습니다.

완료: 새 코드 작성-빌드 절차 정리(2/4)

좋습니다. 지금 워크스페이스 기준으로, 새 코드 작성부터 빌드하고 피코 보드에 넣는 전체 과정을 실제 경로로 순서대로 정리해드리겠습니다.

현재 확인된 기준 파일/설정은 아래와 같습니다.

프로젝트 루트: /home/jjy2026/pico_project
현재 기본 소스: picomoter.c
빌드 설정 파일: CMakeLists.txt
현재 보드 기본값: CMakeLists.txt:12 에서 pico2_w
현재 출력 UF2: my_project.uf2
picotool 경로: picotool
전체 작업 순서

코드 작성
CMake Configure
Build
UF2 파일 확인// 파일 이름은 my_project.uf2
BOOTSEL 모드로 보드 연결
UF2 복사
자동 재부팅/실행 확인
아래 순서 그대로 하시면 됩니다.

1) 코드 작성
현재는 picomoter.c를 기본 소스로 빌드하도록 되어 있으니, 이 파일을 수정하면 됩니다.

필수 조건:

main 함수가 있어야 함
Pico SDK 헤더 포함 필요 시 include
끝에서 무한 루프 또는 종료 처리 명확히
2) 프로젝트 루트로 이동
//home/jjy2026/pico_project 로 이동해서 CMake 명령 실행할 준비
3) CMake Configure
기존에 만든 보드 전용 빌드 폴더 build_pico2w를 사용합니다.

설명:

-S . : 현재 폴더의 CMakeLists.txt 사용
-B build_pico2w : 빌드 산출물은 /home/jjy2026/pico_project/build_pico2w 에 생성
compile_commands.json 생성으로 IntelliSense도 안정화
4) 빌드

빌드 성공 시 핵심 산출물:

my_project.elf
my_project.uf2
5) UF2 생성 확인

파일이 보이면 플래시 준비 완료입니다.

6) 피코 보드를 BOOTSEL 모드로 연결

보드 USB 분리
BOOTSEL 버튼 누른 상태로 USB 연결
PC에서 USB 저장장치로 인식되는지 확인
보통 마운트 이름은 RPI-RP2
리눅스에서 자주 보이는 경로:

/media/사용자명/RPI-RP2
/run/media/사용자명/RPI-RP2
7) UF2 복사해서 업로드
가장 안전한 자동 탐색 방식:

정상 동작:

복사 후 몇 초 내로 드라이브가 자동 분리(재부팅)되는 경우가 많음
보드가 새 펌웨어로 부팅됨
8) 대안: picotool로 업로드
현재 시스템에 picotool이 있으므로 아래도 가능:

설명:

load: UF2 로드
-x: 로드 후 실행
새 파일로 코드 짤 때 방법
현재 CMakeLists.txt:27 는 APP_SOURCE를 사용합니다.
즉 새 소스 파일을 만들었다면 configure 때 소스만 바꿔서 빌드할 수 있습니다.

주의:

새 파일 경로는 CMakeLists.txt 기준 상대경로
파일명이 바뀌어도 출력 UF2 이름은 타깃명이 my_project라서 my_project.uf2
자주 막히는 지점 체크리스트

보드 종류가 다름
현재 설정은 pico2_w 기준
만약 pico_w(RP2040)라면 별도 빌드 폴더를 써야 함
기존 캐시 폴더 재사용 시 플랫폼 충돌 오류가 날 수 있음
USB 케이블 문제
충전 전용 케이블이면 드라이브가 안 뜸
데이터 케이블 필요
BOOTSEL 진입 실패
버튼 누른 상태에서 연결해야 함
연결 후에 누르면 안 잡힐 수 있음
UF2 복사 경로 오타
반드시 my_project.uf2 사용
빌드 폴더 혼동
현재 이 프로젝트는 build 대신 build_pico2w 사용 권장
원하시면 다음 답변에서,

pico2_w 기준 “새 파일 템플릿” 하나 만들어서
빌드/업로드까지 딱 5개 명령만 남긴 초간단 버전으로
정리해드리겠습니다.
